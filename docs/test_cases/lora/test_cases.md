<!-- This file is auto-generated by tools/render_markdown.py. Do not edit by hand. -->

# Test Cases — lora

Module: **LORA** | Total: 15
## Summary
| ID | Title | Reqs Verified | Method | Type | Status |
|----|-------|---------------|--------|------|--------|
| SW-TC-LORA-001 | Transmit Raw Byte Payload Over UART | SW-REQ-LORA-001 | Test | Unit | Active |
| SW-TC-LORA-002 | Payload Bytes Passed Through Unmodified | SW-REQ-LORA-002 | Test | Unit | Active |
| SW-TC-LORA-003 | Transmit Call Completes Within 2 Hz Budget | SW-REQ-LORA-003 | Test | Unit | Active |
| SW-TC-LORA-004 | AT-Command Configuration at Initialization | SW-REQ-LORA-004 | Test | Unit | Active |
| SW-TC-LORA-005 | POST Probe Succeeds When Module Returns OK | SW-REQ-LORA-005 | Test | Unit | Active |
| SW-TC-LORA-006 | POST Fails When Module Does Not Respond | SW-REQ-LORA-005, SW-REQ-LORA-008 | Test | Unit | Active |
| SW-TC-LORA-007 | Health Reflects Most Recent Transmit Outcome | SW-REQ-LORA-006 | Test | Unit | Active |
| SW-TC-LORA-008 | Unhealthy After Send Failure | SW-REQ-LORA-007 | Test | Unit | Active |
| SW-TC-LORA-009 | Unhealthy When Module Fails to Respond to AT | SW-REQ-LORA-008 | Test | Unit | Active |
| SW-TC-LORA-010 | Transmit Returns Non-Success on Failure | SW-REQ-LORA-009 | Test | Unit | Active |
| SW-TC-LORA-011 | Equivalent API Surface POSIX vs Pico2 | SW-REQ-LORA-010 | Test | Unit | Active |
| SW-TC-LORA-012 | Deterministic Output for Identical Inputs | SW-REQ-LORA-011 | Test | Unit | Active |
| SW-TC-LORA-013 | Configurable UART Baud Rate Honored | SW-REQ-LORA-012 | Test | Unit | Active |
| SW-TC-LORA-014 | Pico2 Hardware POST Demonstration | SW-REQ-LORA-005, SW-REQ-LORA-010 | Demonstration | Demonstration | Draft |
| SW-TC-LORA-015 | Pico2 Hardware 2 Hz Downlink Demonstration | SW-REQ-LORA-001, SW-REQ-LORA-003, SW-REQ-LORA-006 | Demonstration | Demonstration | Draft |

## Details
### **SW-TC-LORA-001** — Transmit Raw Byte Payload Over UART

- **Requirements Verified:** SW-REQ-LORA-001
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib with a POSIX mock UART. Module is configured and idle.
- **Acceptance Criteria:** The mock UART captures an AT+SEND transmission whose payload region matches the caller-supplied bytes byte-for-byte and length-for-length, and the transmit call returns JUNO_STATUS_SUCCESS.
- **Procedure:**
  1. Construct a caller-supplied byte buffer of known content and length (e.g., 16 bytes with mixed binary values).
  2. Invoke the lora_lib transmit function with the buffer and length.
  3. Capture the bytes written to the mock UART by the driver.
  4. Inspect the written bytes for the AT+SEND command framing followed by the caller payload bytes.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-002** — Payload Bytes Passed Through Unmodified

- **Requirements Verified:** SW-REQ-LORA-002
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib with a POSIX mock UART. Prepare a payload containing all 256 distinct byte values (0x00 through 0xFF).
- **Acceptance Criteria:** Extracted payload bytes equal the input buffer for both the all-byte-values test and the embedded-AT-substring test, demonstrating no interpretation or modification of caller bytes.
- **Procedure:**
  1. Invoke the lora_lib transmit function with the 256-byte payload.
  2. Extract the payload region from the bytes captured by the mock UART.
  3. Compare the extracted payload bytes to the original input buffer byte-for-byte.
  4. Repeat with a payload containing AT-command-like substrings (e.g., 'AT+SEND', '\r\n') embedded in the data.
  5. Verify the extracted payload still matches the input bytes exactly.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-003** — Transmit Call Completes Within 2 Hz Budget

- **Requirements Verified:** SW-REQ-LORA-003
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib with a POSIX mock UART that simulates RYLR896 OK responses with realistic latency.
- **Acceptance Criteria:** The maximum measured elapsed time across 100 transmit calls is strictly less than 500 ms (the 2 Hz period), and every call returns JUNO_STATUS_SUCCESS.
- **Procedure:**
  1. Record a monotonic timestamp T0 immediately before invoking the lora_lib transmit function.
  2. Invoke the transmit function with a representative telemetry-sized payload.
  3. Record monotonic timestamp T1 immediately after the transmit function returns.
  4. Compute elapsed = T1 - T0.
  5. Repeat for at least 100 successive transmit calls and record the maximum elapsed value.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-004** — AT-Command Configuration at Initialization

- **Requirements Verified:** SW-REQ-LORA-004
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Prepare a POSIX mock UART that records every byte written to it. Provide a configuration struct specifying address, network ID, frequency, spreading factor, and bandwidth.
- **Acceptance Criteria:** All four AT configuration commands are issued during initialization with the values supplied in the configuration struct, and the initialization call returns JUNO_STATUS_SUCCESS.
- **Procedure:**
  1. Invoke the lora_lib initialization function with the configuration struct.
  2. Capture all bytes written to the mock UART during initialization.
  3. Parse the captured bytes into discrete AT command lines.
  4. Verify presence of an AT+ADDRESS command containing the configured address value.
  5. Verify presence of an AT+NETWORKID command containing the configured network ID.
  6. Verify presence of an AT+BAND command containing the configured frequency.
  7. Verify presence of an AT+PARAMETER command containing the configured spreading factor and bandwidth.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-005** — POST Probe Succeeds When Module Returns OK

- **Requirements Verified:** SW-REQ-LORA-005
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Configure the POSIX mock UART to respond to any AT probe with the bytes '+OK\r\n'.
- **Acceptance Criteria:** An AT probe command is written to the UART before any other configuration, and the POST function returns JUNO_STATUS_SUCCESS when the module replies +OK.
- **Procedure:**
  1. Invoke the lora_lib POST/initialization function.
  2. Inspect the bytes written to the mock UART for an AT probe command (bare 'AT' or equivalent).
  3. Inspect the POST return status.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-006** — POST Fails When Module Does Not Respond

- **Requirements Verified:** SW-REQ-LORA-005, SW-REQ-LORA-008
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Configure the POSIX mock UART so reads return no data (timeout) for any AT command.
- **Acceptance Criteria:** The POST function returns a non-success JUNO_STATUS_T error, and the subsequent health query reports an unhealthy state.
- **Procedure:**
  1. Invoke the lora_lib POST/initialization function.
  2. Capture the return status.
  3. Query the lora_lib health status function.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-007** — Health Reflects Most Recent Transmit Outcome

- **Requirements Verified:** SW-REQ-LORA-006
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib with a programmable POSIX mock UART that can be switched between OK and timeout response modes.
- **Acceptance Criteria:** Health is healthy after the first call, transitions to unhealthy after the failing call, and transitions back to healthy after the recovery call, demonstrating health tracks the most recent outcome.
- **Procedure:**
  1. Set the mock UART to OK-response mode and call transmit; query health and record the value.
  2. Set the mock UART to timeout mode and call transmit; query health and record the value.
  3. Set the mock UART back to OK-response mode and call transmit; query health and record the value.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-008** — Unhealthy After Send Failure

- **Requirements Verified:** SW-REQ-LORA-007
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib successfully. Configure the POSIX mock UART to refuse the next transmit (simulate write error or +ERR response).
- **Acceptance Criteria:** Health status is healthy before the failing transmit and is unhealthy immediately after the failing transmit returns.
- **Procedure:**
  1. Verify lora_lib reports healthy status before the failing call.
  2. Invoke the lora_lib transmit function and observe the non-success return status.
  3. Query the lora_lib health status function.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-009** — Unhealthy When Module Fails to Respond to AT

- **Requirements Verified:** SW-REQ-LORA-008
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib successfully. Configure the POSIX mock UART so the next AT command read times out.
- **Acceptance Criteria:** The lora_lib health query reports unhealthy after the AT command times out without a module response.
- **Procedure:**
  1. Trigger an operation that issues an AT command (e.g., transmit or status query).
  2. Observe the non-success return status caused by the missing module response.
  3. Query the lora_lib health status function.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-010** — Transmit Returns Non-Success on Failure

- **Requirements Verified:** SW-REQ-LORA-009
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize lora_lib successfully. Configure the POSIX mock UART to inject a write failure on the next transmit.
- **Acceptance Criteria:** The first call returns a non-JUNO_STATUS_SUCCESS error code, and the recovery call returns JUNO_STATUS_SUCCESS, demonstrating failure is observably reported via status.
- **Procedure:**
  1. Invoke the lora_lib transmit function with a valid payload.
  2. Capture the JUNO_STATUS_T return value.
  3. Reset the mock UART to OK-response mode.
  4. Invoke transmit again and capture the new return value.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-011** — Equivalent API Surface POSIX vs Pico2

- **Requirements Verified:** SW-REQ-LORA-010
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** CMake build configurations exist for both POSIX and Pico2 backends of lora_lib, sharing a common public header. A link harness object file references the full public lora_lib API.
- **Acceptance Criteria:** Both backend link attempts exit with status 0; the header-symbol-checker translation unit compiles without errors or static_assert failures; the nm diff of public lora_lib symbols between POSIX and Pico2 builds is empty, indicating identical exported function signatures.
- **Procedure:**
  1. Build the link harness client object file once against the public lora_lib header.
  2. Invoke the link harness to attempt linking the same client object file against the POSIX build of lora_lib and capture the linker exit status.
  3. Invoke the link harness to attempt linking the same client object file against the Pico2 build of lora_lib and capture the linker exit status.
  4. Compile a header-symbol-checker translation unit that uses static_assert and SFINAE detection idioms to verify each declared lora_lib public API function exists with an identical signature in both backend builds.
  5. Run nm against the POSIX and Pico2 lora_lib library archives, filter to public lora_lib symbols, and diff the two filtered symbol lists.
  6. Capture the static_assert/SFINAE compile result, the two link exit statuses, and the nm diff output as test assertions.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-012** — Deterministic Output for Identical Inputs

- **Requirements Verified:** SW-REQ-LORA-011
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Initialize two independent lora_lib instances with identical configuration and identical POSIX mock UART scripts.
- **Acceptance Criteria:** The two instances produce byte-for-byte identical UART output streams and identical status code sequences for the identical input sequence.
- **Procedure:**
  1. Drive both instances through the same ordered sequence of initialization, transmit, and health-query calls with identical input bytes.
  2. Capture the bytes written by each instance to its mock UART.
  3. Capture the return status of every call from each instance.
  4. Compare the two captured byte streams and the two status sequences.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-013** — Configurable UART Baud Rate Honored

- **Requirements Verified:** SW-REQ-LORA-012
- **Verification Method:** Test
- **Type:** Unit
- **Status:** Active
- **Setup:** Provide a POSIX mock UART that records the baud rate it was configured with.
- **Acceptance Criteria:** The mock UART's recorded baud rate matches the value supplied to lora_lib initialization in both 9600 and 115200 cases.
- **Procedure:**
  1. Invoke lora_lib initialization with a baud rate parameter of 9600.
  2. Read back the mock UART's recorded baud rate.
  3. Tear down and re-initialize lora_lib with a baud rate parameter of 115200.
  4. Read back the mock UART's recorded baud rate again.
- **Expected Artifacts:** None
- **Google Test Ref:** libs/lora_lib/tests/lora_lib_test.cpp

### **SW-TC-LORA-014** — Pico2 Hardware POST Demonstration

- **Requirements Verified:** SW-REQ-LORA-005, SW-REQ-LORA-010
- **Verification Method:** Demonstration
- **Type:** Demonstration
- **Status:** Draft
- **Setup:** Pico2 board with RYLR896 LoRa module wired to the configured UART pins, powered from USB, with serial console attached at 115200 baud.
- **Acceptance Criteria:** The captured serial log shows a lora_lib POST success message within 5 seconds of boot, indicating the RYLR896 responded to the AT probe.
- **Procedure:**
  1. Power on the Pico2 board with the RYLR896 module attached.
  2. Open the serial console and capture the boot log to a file.
  3. Observe the lora_lib POST log line indicating the AT probe was issued and an OK was received.
  4. Photograph the wiring of the Pico2 to the RYLR896 module for the test record.
  5. Save the captured serial console log.
- **Expected Artifacts:**
  - lora_post_serial.log (log): Serial console capture from boot showing lora_lib POST probe and OK response
  - lora_wiring.jpg (photo): Photo of Pico2 to RYLR896 wiring used for the demonstration
- **Google Test Ref:** None

### **SW-TC-LORA-015** — Pico2 Hardware 2 Hz Downlink Demonstration

- **Requirements Verified:** SW-REQ-LORA-001, SW-REQ-LORA-003, SW-REQ-LORA-006
- **Verification Method:** Demonstration
- **Type:** Demonstration
- **Status:** Draft
- **Setup:** Pico2 board with RYLR896 module configured and a second RYLR896 receiver attached to a logging host. Both modules pre-configured to matching address, network ID, frequency, spreading factor, and bandwidth.
- **Acceptance Criteria:** The receiver log shows at least 110 received frames over the 60 second window with mean inter-arrival time within 500 ms +/- 50 ms, and payload bytes match expected content.
- **Procedure:**
  1. Power on the Pico2 transmitter and start the telemetry app that exercises lora_lib at 2 Hz.
  2. Start logging on the receive host, recording timestamps of received +RCV lines for at least 60 seconds.
  3. Stop logging and save the receiver log file.
  4. Compute inter-arrival times between successive received frames.
  5. Verify each received payload byte sequence matches the transmitter's expected sequence.
- **Expected Artifacts:**
  - lora_rx_2hz.log (log): Receiver host log of timestamped +RCV lines from the RYLR896
  - lora_inter_arrival.csv (csv): CSV of frame index and inter-arrival time computed from rx log
- **Google Test Ref:** None

